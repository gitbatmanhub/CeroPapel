{{#if user}}

<div class="container p-4">
    <div class="row">
        <div class="card text-center">
            <div class="card-body">
                <h3>Welcome {{user.fullname}}</h3>
                <h3>User: {{user.username}}</h3>
                <a href="/orden/agregarOrden" class="btn btn-primary">Agrega Orden de mantenimiento</a>
            </div>
        </div>
    </div>
</div>
{{else}}
    <h1>Ingresa o registrate prro</h1>

{{/if}}